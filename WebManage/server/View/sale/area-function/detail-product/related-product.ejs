<div class="related-product">
  <h2 class="related-product-caption text-center pb-4">RELATED PRODUCT</h2>
  <div class="row best-seller-group grid" id="relase-info-product">
    <div class="best-seller-item"></div>
  </div>
</div>

<script type="text/javascript">
  function dataAPIForSliderInfo(element) {
    var dataInfor = `<div class="best-seller-item">
                          <div class="image-block-view">
                            <div class="image-block-view-250">
                              <img src="${
                                element.link_url
                              }"  class="best-seller-item-image img-fluid" alt="" />
                            </div>
                          </div>
                          <div class="best-seller-item-info">
                              <span><a class="info-name" href="/sale/${setPadamDetailSale(
                                element
                              )}">
                                    ${element.title}</a>
                              </span> <!-- 
                              <div class="info-star">
                                  <i class="fa fa-star"></i>
                                  <i class="fa fa-star"></i>
                                  <i class="fa fa-star"></i>
                                  <i class="fa fa-star"></i>
                                  <i class="fa fa-star"></i>
                              </div>  -->
                              <div class="info-price">
                                  <span class="info-price-discount">${
                                    element.mid_cost
                                  }VND</span>
                              </div>
                          </div>
                     </div>`;
    return dataInfor;
  }
  const urlParams = new URLSearchParams(window.location.search);
  var product_block = urlParams.get("block");
  if (isNaN(parseInt(product_block))) product_block = 1;
  getInfoHtml(
    "/api/customers/info_product?type=" + product_block + "&ver=1",
    (data) => {
      var htmlItem = "";
      data.result.forEach((element, index) => {
        if (index < 6) htmlItem += dataAPIForSliderInfo(element);
      });
      $("#relase-info-product").html(htmlItem);
    }
  );
  /*postInfoData('/api/customers/lst_product',lstGroupElectric,(data)=>{
              var product_group_id=1;
              data.result.forEach((element) => {
                if(jsonItemElectric.findIndex(o=>(o.product_id==element.product_id))<0) jsonItemElectric.push(element);
                product_group_id=element.product_group_id;
              });
              setFillertProductToView(jsonItemElectric,"#best-seller-items");
              
            });*/
</script>
